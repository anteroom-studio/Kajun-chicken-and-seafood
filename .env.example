# KAI server-side proxy reads this from the runtime environment.
# It is NEVER read by the browser bundle (no VITE_ prefix on purpose).
# In Vercel, set this under Project Settings -> Environment Variables.
# Locally, copy to .env (already gitignored).
GROQ_API_KEY=gsk_your_real_key_here
